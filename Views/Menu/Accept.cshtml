@model FPTBook.Models.CategoryReq

@{
    ViewData["Title"] = "Accept Request";
}

<h1>Accept Request</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Name)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Name)</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Req)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Req)</dd>
    </dl>

    <form asp-action="Accept">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Accept" class="btn btn-danger" />
    </form>
</div>